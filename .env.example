# Lovable Cloud injects these automatically in the Lovable editor and in
# deployed builds. Copy to `.env` only if you want to run `npm run dev`
# locally against the real database.
#
# Find them in the Lovable editor under Cloud → Settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
